Hello brother, here I'm actually designing the manager and agent binaries right Hope you got it See I will provide you the information very clearly but don't rush OK don't try to understand everything at once if you're not understanding and refer and refine if required because this is just these are just my intuitions I don't know whether these are top notch or not but I'm thinking that these are very nice see let me tell you I am actually creating the Kubernetes like architecture Kubernetes like architecture doesn't mean it includes the pods and containers no it's not it's completely different It is actually with micro VM Kubernetes actually has two types of nodes master node and worker We have two types of binaries manager binary and agent binary The manager binary is responsible for creating the clusters It can create multiple clusters in the same machine and the agent binary is responsible joining in the clusters that created by the master binary and one machine binary when one machine can actually join in multiple clusters OK so this is what and we are not managing the proxy nonsense and other things that is too bloat Instead we are actually utilizing net bird dull as our source of truth We don't handle certificates these kind of things we don't never handle Let me tell you how the flow will be The user actually first installs the manager binary so he wants him in he just do do common things either create the create a new cluster completely or else join in the existing cluster basically to either create or join in cluster Fundamentally it requires the net Bird API key The reason is when you create the cluster the first thing it needs to do is create the group and policies within the netburb And if possible it will also keep the metadata of that cluster directly within Net bird Join in the existing cluster Meals If you provide the netbat token it will search about the existing clust because it actually creates clusters with specific pattern It is just as simple as that So here after creating or joining in the cluster it needs to the most important Which provisioning the API server API server actually requires the database you know it will rotate both in micro VMS You got my point Here after creating or deleting Are you joining or leaving You know the cluster level management already happened right So once you create the cluster you actually need to join the AG for this Isn't joining You know our manager itself is responsible for creating different types of tokens Basically you know about two types of tokens first token is about used by agents searched to join in the cluster you know since it had I mean since the manager have access to the API token it will simply generate the setup token that you actually pass from the agent it will automatically inside cluster maybe with specific group automatically Or else our API server component itself is very intell Bit but it acted as middle guy you know that's all one thing API server has its own SDK and the manager actually uses that SDK to contact the API server since the API server is just created by the manager and this all available in the same network measure it will simply access the API server directly so by using that SDK it targets the endpoint in the same same mesh network so to actually create the access tokens access token is designed for getting the axis of that cluster you know we we create access tokens with specific roles So while creating the access token he can actually select what roles he want he needs to provide from the manager directly So the manager literally provides the tokens if you want admin token it will provide OK so simply you install another binary which is a controller it is it is same binary as KUBECTL in a similar to but we don't have any kubernetes configuration or something we just have a simple token that with permissions OK as simple as that and our key our controller actually can run the UI itself you know it comes with UI and CLI as a single binary I hope you got my point brother I think its all very clear now So what is your take on these points here my point is not understand implement the complete ecosystem at once but instead we have to think from the manager perspective and the agent perspective because these are the two things right Because if we establish these two things and if you clarify that doubt that the manager can able to create the setup to automatically join in the specific group with specific policies within the network Mes we will directly use the manager for that so in that case we we will we can actually able to design the agent binary and manager binary not directly with code at least we can finalize the commands at least right so once we finalized that we can think about the API server You know because the logic is important you cannot implement something because these are these are all dependent on each other For example the manager actually requires the API server SDK to actually generate the token with specific permissions so I cannot implement as chunks to avoid that I will design all at once but you know but the concepts can I mean we can establish the code is I mean the code is dependent but logic is not you know we can actually finalize the commands of binaries before before thinking about the API server and its capabilities and its work So please tell me your opinion on on these points and tell me if you're ready to move on to the A manager CLA tell me I'm not asking you Provide every command at once but tell me if you are ready so as I told you you don't never ever rush