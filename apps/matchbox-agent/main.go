// Matchbox Agent — Worker node binary.
//
// The agent runs on each worker node and is responsible for executing
// workloads assigned by the cluster. It exposes its own CLI interface.
package main

func main() {
	// TODO: Initialize agent CLI and runtime
}
